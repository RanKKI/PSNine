package xyz.rankki.psnine.common.config

import android.graphics.Color

class RefreshColors {
    companion object {
        const val ColorA: Int = Color.RED
        const val ColorB: Int = Color.BLUE
        const val ColorC: Int = Color.GREEN
    }
}