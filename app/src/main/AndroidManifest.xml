<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="club.ranleng.psnine">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.Main.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.Main.ArticleActivity"
            android:label="@string/title_activity_article"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity.Main.NoticeActivity" />
        <activity android:name=".activity.Main.LoginActivity" />
        <activity
            android:name=".activity.Post.ReplyActivity"
            android:windowSoftInputMode="stateVisible|adjustResize" />
        <activity android:name=".activity.Assist.ImageActivity" />
        <activity
            android:name=".activity.Assist.AboutActivity"
            android:label=""
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity.Assist.PickImgActivity" />
        <activity
            android:name=".activity.Main.PersonInfoActivity"
            android:label="@string/title_activity_person_info"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity.Assist.SettingActivity" />
        <activity android:name=".activity.Assist.SearchActivity" />
        <activity android:name=".activity.Post.NewGeneActivity"
            android:windowSoftInputMode="adjustResize" />
    </application>

</manifest>