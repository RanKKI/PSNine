<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="club.ranleng.psnine">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">
        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="@string/authProvider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_path"/>
        </provider>

        <activity
            android:name=".ui.main.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.Animation"/>
        <activity
            android:name=".ui.topic.TopicActivity"
            android:label="@string/title_activity_topic"
            android:theme="@style/AppTheme.Animation">
        </activity>
        <activity android:name=".common.LinkHandleActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>

                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>

                <data
                    android:host="psnine.com"
                    android:scheme="http"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.ImageViewActivity"
            android:theme="@style/AppTheme.Animation"/>
        <activity
            android:name=".ui.LoginActivity"
            android:theme="@style/AppTheme.Animation"/>
        <activity
            android:name=".ui.setting.SettingsActivity"
            android:theme="@style/AppTheme.Animation"/>
        <activity
            android:name=".ui.topics.TopicsActivity"
            android:theme="@style/AppTheme.Animation"/>
        <activity
            android:name=".ui.newTopic.newTopicActivity"
            android:theme="@style/AppTheme.Animation"/>
        <activity
            android:name=".ui.imageGallery.ImagesGalleryActivity"
            android:theme="@style/AppTheme.Animation"/>

        <activity
            android:name=".ui.main.InitActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>